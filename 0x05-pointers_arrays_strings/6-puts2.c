#include "main.h"
/**
 * puts2 - prints every other character of a string.
 * @str: The string to use
 * Return: void
 */
void puts2(char *str)
{
int i;

for (i = 0; str[i] != '\0'; i++)
{

if (a % 2 == 0)
_putchar (str[i]);
}

_putchar ('\n');
}
